# Optional profile assets

这里仅保存经过审计、可供任意用户使用的桌面模板。模板必须使用 `$HOME` 和 XDG
路径，并由 first-login 流程应用；package 不得直接修改任意现有 `/home/*`。

个人背景、browser profile、SSH key、network connection、Tailscale/npm/GitHub/
Codex state 和 fixed username 不属于 image 输入。
